{
  "importMap": "./import_map.json",
  "tasks": {
    "gen-keys": "rm -rf keys && mkdir keys && deno run -A --unstable ./scripts/gen-keys.ts",
    "migrate": "deno run -A --unstable ./persistence/kysely/migrator.ts",
    "start": "deno run -A --unstable ./src/index.ts",
    "start:dev": "denon run -A --unstable ./src/index.ts"
  }
}
